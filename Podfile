# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Meteo App' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Meteo App
  # Networking layer
  pod 'Alamofire'
  pod 'PromisesSwift'
  pod 'SwiftyJSON'
  
  target 'Meteo AppTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'Meteo AppUITests' do
    # Pods for testing
  end

end
